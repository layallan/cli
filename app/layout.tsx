import type { Metadata } from "next";
import "./globals.css";

const basePath = process.env.NEXT_PUBLIC_BASE_PATH ?? "";

export const metadata: Metadata = {
  title: {
    default: "Center of Light Intelligence Research Lab",
    template: "%s | Center of Light Intelligence Research Lab",
  },
  description:
    "The Center of Light Intelligence Research Lab develops new ways to program light, interrogate matter, infer hidden physical states, and adapt experiments.",
  icons: {
    icon: `${basePath}/favicon.svg`,
    shortcut: `${basePath}/favicon.svg`,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
